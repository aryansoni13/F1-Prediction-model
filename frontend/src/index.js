import React from 'react';
import ReactDOM from 'react-dom/client';
import F1PredictionDashboard from './F1PredictionDashboard';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <F1PredictionDashboard />
  </React.StrictMode>
); 